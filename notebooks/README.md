# Notebooks

Exploration, backtests, and model diagnostics. Empty until Phase 2.

Reproducible extracts belong in `src/`. Notebooks should import those scripts rather than becoming the only copy of transform logic.
