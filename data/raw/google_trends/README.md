# Raw Google Trends

One CSV per confirmed keyword, pulled with `src/pull_google_trends.py` (`today 5-y`, geo=US).

`isPartial=True` on the last week means Google has not finalized that week. Keep the row; do not drop it.
