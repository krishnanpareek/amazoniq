# Processed data

Cleaned dimension and fact tables written by `src/` scripts. Empty until Phase 2.

Every processed file should remain traceable to a `Source_ID` in [`../../docs/source_inventory.md`](../../docs/source_inventory.md).
